import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.arcade.llamaswithhats',
    appName: 'Llamas with Hats: The Incident',
    webDir: 'dist',
    bundledWebRuntime: false
};

export default config;
